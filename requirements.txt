# Phoenix – core
flask>=2.3
requests>=2.31

# Phoenix – voice input (Phase 5.2)
# Uncomment to enable /voice endpoint:
# faster-whisper>=1.0

# Phoenix – fine-tune pipeline (Phase 6.2)
# No extra deps — uses ollama CLI directly
